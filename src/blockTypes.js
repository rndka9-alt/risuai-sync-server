'use strict';

// ---------------------------------------------------------------------------
// RisuSave 블록 타입 상수 & 동기화 상수
// 클라이언트에서도 사용됨 (client-builder.js 에서 주입)
// @see ./client/sync.js
// ---------------------------------------------------------------------------
// 0: CONFIG   – 설정 데이터
// 1: ROOT     – 최상위 DB 블록 (__directory, enabledModules 등)
// 2: WITH_CHAT    – 캐릭터 카드 (채팅 포함)
// 6: REMOTE   – 원격 블록 (Phase 1 fallback 대상)
// 7: WITHOUT_CHAT – 캐릭터 카드 (채팅 미포함)
// ---------------------------------------------------------------------------
const BLOCK_TYPE = {
  CONFIG: 0,
  ROOT: 1,
  WITH_CHAT: 2,
  REMOTE: 6,
  WITHOUT_CHAT: 7,
};

// ---------------------------------------------------------------------------
// 라이브 적용 가능한 ROOT 키 화이트리스트
// ---------------------------------------------------------------------------
const SAFE_ROOT_KEYS = ['enabledModules'];

module.exports = { BLOCK_TYPE, SAFE_ROOT_KEYS };
